[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --cov=app
    --cov=main
    --cov-report=term-missing
    --cov-fail-under=85

markers =
    slow: testes de integração mais pesados
